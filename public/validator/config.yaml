# This is currently how the app is configured to test different connectors
# When the app is deployed to an environment i.e. foo.com/ this value should
# be updated.
core_host: http://matthews-imac.local:9000
username: ALG12
